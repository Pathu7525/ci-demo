---
name: My Workflow
on:
  workflow_dispatch:
    inputs:
      name:
        description: Optional input parameter
        required: false
jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Run a step
        run: 'echo "Running workflow with input: ${{ github.event.inputs.name }}"'
